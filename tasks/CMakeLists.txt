# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(gpsTaskLib)
ADD_LIBRARY(${GPS_TASKLIB_NAME} SHARED 
    ${GPS_TASKLIB_SOURCES})

set(CMAKE_MODULE_PATH ${PROJECT_SOURCE_DIR})
find_package(GDAL REQUIRED)
add_definitions(${GDAL_CFLAGS})
include_directories(${GDAL_INCLUDE_DIRS})
link_directories(${GDAL_LIBRARY_DIRS})

add_dependencies(${GPS_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${GPS_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${GPS_TASKLIB_DEPENDENT_LIBRARIES}
    ${GDAL_LIBRARIES})

SET_TARGET_PROPERTIES(${GPS_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${GPS_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${GPS_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${GPS_TASKLIB_HEADERS}
    DESTINATION include/orocos/gps)

